import 'package:flutter/material.dart';

class NFTMall extends StatelessWidget {
  const NFTMall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('NFT 몰'),
    );
  }
}
